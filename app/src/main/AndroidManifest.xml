<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ipg.healthmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".lista_medicos"></activity>
        <activity android:name=".regista_medico" />
        <activity android:name=".lista_Pacientes" />
        <activity android:name=".Ecra_principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ficha_Paciente" />
        <activity android:name=".Dados_paciente" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>